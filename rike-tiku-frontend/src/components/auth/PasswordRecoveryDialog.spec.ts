// @vitest-environment jsdom
import {mount} from '@vue/test-utils';import {beforeEach,describe,expect,it,vi} from 'vitest';import PasswordRecoveryDialog from './PasswordRecoveryDialog.vue'
const api=vi.hoisted(()=>({requestPasswordRecovery:vi.fn()}));vi.mock('../../api/auth',()=>api);vi.mock('element-plus',()=>({ElMessage:{success:vi.fn(),warning:vi.fn()}}));const stubs={ElDialog:{template:'<div><slot/></div>'},ElForm:{template:'<form @submit.prevent="$emit(\'submit\')"><slot/></form>'},ElFormItem:{template:'<label><slot/></label>'},ElInput:true,ElButton:{template:'<button type="submit"><slot/></button>'},ImageCaptcha:{template:'<div>图形验证码</div>'}}
describe('密码恢复对话框',()=>{beforeEach(()=>api.requestPasswordRecovery.mockReset());it('只收集用户名和验证码并展示反枚举说明',()=>{const wrapper=mount(PasswordRecoveryDialog,{props:{modelValue:true,'onUpdate:modelValue':vi.fn()},global:{stubs}});expect(wrapper.text()).toContain('系统不会在此处判断账号是否存在');expect(wrapper.text()).toContain('图形验证码');expect(wrapper.text()).not.toContain('旧密码')})})
