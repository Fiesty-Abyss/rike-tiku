// @vitest-environment jsdom
import {flushPromises,mount} from '@vue/test-utils';import {beforeEach,describe,expect,it,vi} from 'vitest';import StudentAiVariantPanel from './StudentAiVariantPanel.vue'
const api=vi.hoisted(()=>({generateAiVariant:vi.fn(),answerAiVariant:vi.fn(),discardAiVariant:vi.fn(),submitAiVariantReview:vi.fn()}));vi.mock('../../api/student/aiLearning',()=>api);vi.mock('element-plus',()=>({ElMessage:{warning:vi.fn()}}));const variant={id:8,answerFactId:3,motherQuestionId:1,questionId:9,status:'READY',questionType:'SINGLE_CHOICE',stem:'**电场力**满足 \\(F=qE\\)',difficulty:2,options:[{label:'A',content:'沿电场方向'},{label:'B',content:'垂直电场'}],reviewStatus:'PENDING'};const stubs={AiScientificContent:{props:['content'],template:'<div>{{content}}</div>'},ElButton:{props:['loading'],template:'<button @click="$emit(\'click\')"><slot/></button>'},ElCheckboxGroup:{template:'<div><slot/></div>'},ElCheckbox:true,ElRadioGroup:{template:'<div><slot/></div>'},ElRadio:{props:['value'],template:'<label>{{value}}<slot/></label>'},ElInput:true,ElTag:{template:'<span><slot/></span>'}}
describe('学生 AI 变式练习',()=>{beforeEach(()=>{vi.clearAllMocks();api.generateAiVariant.mockResolvedValue(variant);api.answerAiVariant.mockResolvedValue({...variant,status:'ANSWERED',correct:true,aiAnalysis:'AI 解析',correctAnswer:{optionLabels:['A']}})})
it('展示完整选择项并在作答后标记 AI 解析',async()=>{const wrapper=mount(StudentAiVariantPanel,{props:{answerFactId:3},global:{stubs}});await wrapper.get('button').trigger('click');await flushPromises();expect(wrapper.text()).toContain('A. 沿电场方向');expect(wrapper.text()).toContain('B. 垂直电场');const submit=wrapper.findAll('button').find(item=>item.text()==='提交答案');await submit!.trigger('click');await flushPromises();expect(wrapper.text()).toContain('回答正确');expect(wrapper.text()).toContain('AI 生成解析')})})
