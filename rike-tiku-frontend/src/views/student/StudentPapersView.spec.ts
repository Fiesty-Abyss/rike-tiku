import {mount,flushPromises} from '@vue/test-utils';import {beforeEach,describe,expect,it,vi} from 'vitest';import StudentPapersView from './StudentPapersView.vue'
const api=vi.hoisted(()=>({list:vi.fn(),detail:vi.fn(),draft:vi.fn(),submit:vi.fn(),push:vi.fn()}));vi.mock('../../api/student/papers',()=>({fetchStudentPapers:api.list,fetchStudentPaper:api.detail,saveStudentPaperDraft:api.draft,submitStudentPaper:api.submit}));vi.mock('vue-router',()=>({useRoute:()=>({params:{releaseId:'7'}}),useRouter:()=>({push:api.push})}));vi.mock('element-plus',()=>({ElMessage:{success:vi.fn()},ElMessageBox:{confirm:vi.fn().mockResolvedValue(true)}}));
describe('学生冻结试卷',()=>{beforeEach(()=>{api.list.mockResolvedValue([]);api.detail.mockResolvedValue({release:{id:7,paperName:'199班物理',subjectName:'物理',className:'199班',submissionStatus:'NOT_STARTED'},answersVisible:false,questions:[{itemId:3,order:1,score:10,type:'SINGLE_CHOICE',stem:'选择正确项',answerSlots:1,options:[{label:'A',content:'选项 A'}],submittedAnswer:null,knowledgePoints:['力学']} ]})});it('作答前隐藏 STANDARD 并显示确定性判分边界',async()=>{const wrapper=mount(StudentPapersView,{global:{stubs:{ScientificText:{props:['content'],template:'<span>{{content}}</span>'},AnswerDisplay:true,ElButton:{template:'<button><slot/></button>'},ElTag:{template:'<span><slot/></span>'},ElRadioGroup:{template:'<div><slot/></div>'},ElRadio:{template:'<label><slot/></label>'},ElCheckboxGroup:true,ElCheckbox:true,ElInput:true,ElEmpty:true}}});await flushPromises();expect(wrapper.text()).toContain('199班物理');expect(wrapper.text()).toContain('确定性规则判分');expect(wrapper.find('.standard').exists()).toBe(false)})})
