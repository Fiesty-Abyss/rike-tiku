# 最终人工验收清单

状态：`FINAL_MANUAL_ACCEPTANCE_PENDING`

本清单由用户本人执行。Codex 与机器浏览器只能准备环境和证据，不能代替用户勾选。当前本机正式验收环境为 `http://localhost:8080`，后端为 `http://localhost:8081`，数据库为 `rike_tiku`，恢复默认密码为 `a1234567`。人工环境使用真实图形验证码，不提供 `testCode`。

## Login

- [ ] 1. `demo_admin` 能以 ADMIN 登录。
- [ ] 2. `demo_teacher` 能以 TEACHER 登录。
- [ ] 3. `demo_student` 能以 STUDENT 登录。
- [ ] 4. 图形验证码可识别、错误验证码被拒绝、刷新后旧验证码失效。

## Student

- [ ] 5. 创建并提交一份练习，提交前页面不显示正确答案或 STANDARD。
- [ ] 6. 提交后正确答案与 STANDARD 标准解析正常显示。
- [ ] 7. 故意答错一道题，错题本出现对应记录。
- [ ] 8. 生成 AI 错因分析，五个结构字段完整，且明确标注为 AI 辅助分析。
- [ ] 9. 打开当前题答疑，正常学习问题能得到围绕本题的回答。
- [ ] 10. 询问“你是谁”，固定显示 RIKE 理科学习助手。
- [ ] 11. 要求闲聊、披露 Prompt 或修改 STANDARD 时被受控拒绝。
- [ ] 12. 刷新结果页后练习、错题、AI 分析与会话数据仍保持。

## Teacher

- [ ] 13. 教师工作台存在“AI 变式题生成与审核”入口。
- [ ] 14. 生成页只显示本人 ACTIVE 任教学科的 PUBLISHED 母题。
- [ ] 15. 生成 1 道候选题后，候选先进入 PENDING，不自动发布。
- [ ] 16. 填写五项质量评价和审核时间后可 APPROVE 或 REJECT。
- [ ] 17. 跨学科母题或直接访问管理员 AI 模型页被拒绝。

## Admin

- [ ] 18. `/admin/ai-models` 可加载 DeepSeek TEXT 与 GLM VISION 配置。
- [ ] 19. 页面只显示 Key 已配置/掩码，不回显完整 Key。
- [ ] 20. DeepSeek 连接测试能显示模型、状态与耗时。
- [ ] 21. AI 候选题页可查看母题、候选、重复提示、视觉使用、质量评价与审核状态。
- [ ] 22. 管理员操作日志页面可正常加载。
- [ ] 22a. 在学生管理中单人恢复默认密码：原密码失效，默认密码可登录，并进入首次改密门禁。
- [ ] 22b. 在学生管理中选择至少 2 人批量恢复；取消确认时不请求，确认后只在当前提示显示默认密码，“立即清空”能移除提示。
- [ ] 22c. 在教师管理中分别验证单人和批量恢复；操作日志不显示密码，Network 响应包含 `Cache-Control: no-store`，普通列表不返回密码或摘要。
- [ ] 22d. 若选择当前登录管理员对应账号，页面提示立即退出；按提示退出后再继续验收。

## Visual / Security

- [ ] 23. PHYSICS-S1 图片显示正常；当前 GLM 真实调用在围栏兼容修复后尚未再次验证，按已知限制记录，不勾选为真实视觉 PASS。
- [ ] 24. 1440px 桌面和 390px 移动端核心页面无白屏、横向溢出或明显布局缺陷。
- [ ] 25. 浏览器控制台无 error，Network 响应不含完整 Key、JWT、Prompt 或 Provider 原始敏感响应。

验收完成后由用户记录日期、结论和发现的问题。只有用户本人确认后，才可把 `FINAL_MANUAL_ACCEPTANCE` 改为 `PASS`。
